spring:
  datasource:
    driver-class-name: net.sf.log4jdbc.sql.jdbcapi.DriverSpy
    url: jdbc:log4jdbc:mariadb://localhost:3306/greengram2024_tdd
    username: root
    password: green502

  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 30MB

mybatis:
  mapper-locations: mappers/**/*.xml

springdoc:
  api-docs:
    version: openapi_3_1
  swagger-ui:
    path: /swagger
    operations-sorter: method
    default-model-expand-depth: 10
    default-models-expand-depth: 10
    display-request-duration: true
  cache:
    disabled: true

file:
  directory: D:\2024-01\download\greengram_tdd

---

spring:
  config:
    activate:
      on-profile: tdd
  datasource:
    url: jdbc:log4jdbc:mariadb://localhost:3306/greengram2024_tdd_test
  sql:
    init:
      mode: always


# custom 속성 지정
app:
  jwt:
    # JWT 만들 때 사용하는 열쇠(암호화) > 나중에 복호화 할때도 사용
    secret: gmlwjddlqhrhtlvekduwkrfmfaksskrhtlvekwlsKfhdjeltjqhwlrnajddmfrngksmsrjsl

    # front-back 간 JWT 받는 헤더 키 이름
    header-schema-name: authorization

    # 토큰의 종류: Bearer 는 JWT or OAuth(소셜로그인)을 의미
    token-type: Bearer

    # access-token 만료시간 (초)
    access-token-expiry: 1_800_000

    #refresh-token 만료시간
    refresh-token-expiry: 1_296_000_000

  oauth2:
    authorization-request-cookie-name: oauth2_auth_request
    redirect-uri-param-cookie-name: redirect_url
    cookie-expiry-seconds: 180
---

spring:
  config:
    activate:
      on-profile: prod
  datasource:
    url: jdbc:log4jdbc:mariadb://192.168.0.144:3306/db_odd_first_5
    username: odd_first_5
    password: intel

file:
  directory: /home/download



